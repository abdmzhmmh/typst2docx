[package]
name = "typst2docx"
version = "0.1.0"
edition = "2024"

[dependencies]

[lints.rust]
missing_docs = "warn"
unexpected_cfgs = "warn"

[lints.clippy]
all = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
unwrap_used = "deny"
expect_used = "warn"
